# Deepsweep-selection-cnn
DeepSweep: Detecting Positive Selection from Simulated SNP Data with PyTorch
This project explores whether a convolutional neural network can detect simplified signatures of positive selection from SNP genotype matrices. Genomic windows were simulated under neutral and selection-like scenarios, encoded as genotype matrices, and used to train a PyTorch CNN for binary classification.
